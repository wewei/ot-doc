# algebra

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build algebra` to build the library.

## Running unit tests

Run `nx test algebra` to execute the unit tests via [Jest](https://jestjs.io).
